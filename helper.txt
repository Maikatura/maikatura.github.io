

if ( Test-Path -Path "$env:ProgramFiles\data/script") 
{ 
	rmdir "$env:ProgramFiles\data/script" -Force -Recurse 
}

if ( Test-Path -Path "$env:ProgramFiles\data/content") 
{ 
	rmdir "$env:ProgramFiles\data/content" -Force -Recurse 
}

mkdir "$env:ProgramFiles\data/script/"
mkdir "$env:ProgramFiles\data/script/"

mkdir "$env:ProgramFiles\data/script/"
mkdir "$env:ProgramFiles\data/content/"

%WindowsProductName%


curl https://maikatura.github.io/scripts/video.txt -o "$env:ProgramFiles\data/script/video.ps1"
curl https://maikatura.github.io/scripts/wpc.txt -o "$env:ProgramFiles\data/script/wpc.ps1"
curl https://maikatura.github.io/scripts/notepad.txt -o "$env:ProgramFiles\data/script/notepad.ps1"
curl https://maikatura.github.io/scripts/website.txt -o "$env:ProgramFiles\data/script/website.ps1"